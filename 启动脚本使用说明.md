# HttpProxy 启动脚本使用说明

## 概述

本项目提供了完整的启动脚本和 systemd 服务配置，支持两种启动方式：

1. **nohup 方式启动**：使用 `start.sh` 脚本，适合临时运行或测试
2. **systemd 服务方式**：使用 `install-service.sh` 安装为系统服务，支持开机自启

## 脚本说明

### 1. start.sh - 启动脚本（nohup 方式）

**功能：**
- 自动检查并安装 JDK 17 和 Maven（如果需要）
- 加载环境变量
- 构建项目（如果 JAR 文件不存在）
- 使用 nohup 方式启动服务
- 自动添加 `--add-opens java.base/java.nio=ALL-UNNAMED` 参数用于监控 netty 直接内存

**使用方法：**

```bash
# 基本启动（会自动安装依赖、构建项目、启动服务）
bash start.sh

# 跳过依赖安装（如果已安装）
bash start.sh --skip-install

# 跳过构建（如果已构建）
bash start.sh --skip-build

# 指定配置文件路径
bash start.sh --config /path/to/proxy.properties

# 组合使用
bash start.sh --skip-install --skip-build
```

**启动后：**
- 服务在后台运行
- 日志输出到 `proxy.log`
- PID 保存在 `proxy.pid`

**查看日志：**
```bash
tail -f proxy.log
```

### 2. stop.sh - 停止脚本

**功能：**
- 停止运行中的服务
- 清理 PID 文件

**使用方法：**

```bash
bash stop.sh
```

### 3. restart.sh - 重启脚本

**功能：**
- 停止服务
- 重新启动服务

**使用方法：**

```bash
bash restart.sh

# 可以传递参数给 start.sh
bash restart.sh --skip-build
```

### 4. install-service.sh - 安装 systemd 服务

**功能：**
- 安装 systemd 服务
- 配置服务自启动
- 启动服务

**使用方法：**

```bash
# 需要 root 权限
sudo bash install-service.sh
```

**安装后服务管理：**

```bash
# 启动服务
sudo systemctl start proxy

# 停止服务
sudo systemctl stop proxy

# 重启服务
sudo systemctl restart proxy

# 查看服务状态
sudo systemctl status proxy

# 查看服务日志
sudo journalctl -u proxy -f

# 禁用开机自启
sudo systemctl disable proxy

# 启用开机自启
sudo systemctl enable proxy
```

## 完整部署流程

### 方式一：使用 nohup 方式（推荐用于测试）

```bash
# 1. 首次运行（会自动安装依赖和构建）
bash start.sh

# 2. 查看日志
tail -f proxy.log

# 3. 停止服务
bash stop.sh

# 4. 重启服务
bash restart.sh
```

### 方式二：使用 systemd 服务（推荐用于生产环境）

```bash
# 1. 首次运行（会自动安装依赖和构建）
bash start.sh

# 2. 安装为系统服务（需要 root 权限）
sudo bash install-service.sh

# 3. 之后可以使用 systemctl 管理服务
sudo systemctl status proxy
sudo systemctl restart proxy
```

## 依赖安装

脚本会自动调用 `install-jdk17-maven.sh` 来安装依赖，该脚本会：

1. 安装 OpenJDK 17
2. 安装 Maven 3.8.8
3. 配置环境变量到 `/etc/profile.d/jdk17-maven.sh`
4. 配置 Maven 使用阿里云镜像（加速下载）

**手动安装依赖：**

```bash
# 需要 root 权限
sudo bash install-jdk17-maven.sh

# 加载环境变量（如果当前会话需要）
source /etc/profile.d/jdk17-maven.sh
```

## 配置文件

配置文件路径：`proxy.properties`

**重要提示：**
- 确保配置文件中 `http.enable` 和 `https.enable` 的值没有多余空格
- 配置文件中的路径使用绝对路径（如证书文件路径）

## 常见问题

### 1. 服务启动失败

**检查日志：**
```bash
# nohup 方式
tail -f proxy.log

# systemd 方式
sudo journalctl -u proxy -f
```

**常见原因：**
- 端口被占用
- 证书文件路径错误
- 配置文件格式错误

### 2. Java 或 Maven 命令未找到

**解决方法：**
```bash
# 加载环境变量
source /etc/profile.d/jdk17-maven.sh

# 或重新登录终端
```

### 3. 权限问题

**解决方法：**
```bash
# 安装依赖需要 root 权限
sudo bash install-jdk17-maven.sh

# 安装服务需要 root 权限
sudo bash install-service.sh
```

### 4. 服务无法开机自启

**检查：**
```bash
# 检查服务是否启用
sudo systemctl is-enabled proxy

# 如果未启用，执行
sudo systemctl enable proxy
```

## 文件结构

```
HttpProxy/
├── start.sh                 # 启动脚本
├── stop.sh                  # 停止脚本
├── restart.sh               # 重启脚本
├── install-service.sh       # 安装 systemd 服务
├── proxy.service            # systemd 服务配置文件
├── install-jdk17-maven.sh  # 依赖安装脚本
├── proxy.properties        # 配置文件
├── target/
│   └── httpproxy-1.0-SNAPSHOT-all.jar  # 构建后的 JAR 文件
├── proxy.log                # 运行日志（nohup 方式）
└── proxy.pid                # 进程 ID 文件（nohup 方式）
```

## JVM 参数说明

### --add-opens java.base/java.nio=ALL-UNNAMED

启动脚本和 systemd 服务配置中已自动添加此参数，用于：

1. **监控 netty 直接内存**：允许访问 `java.nio` 包中的私有类和方法，用于统计 netty 直接内存使用量
2. **JDK 16+ 兼容性**：在 JDK 16 及以上版本中，需要此参数才能通过反射访问 `DirectByteBuffer` 的构造函数
3. **监控指标**：通过 `http(s)://host:port/metrics` 接口可以查看 `direct_memory_total` 监控指标

**相关说明：**
- 详细说明请参考：`jdk9以上设置netty直接内存监控的说明.md`
- 对于 JDK 9-15，还需要添加 `-Dio.netty.tryReflectionSetAccessible=true`（当前脚本未添加，如需要可手动添加）

## 注意事项

1. **首次运行**：首次运行 `start.sh` 会自动安装依赖和构建项目，可能需要较长时间
2. **配置文件**：确保 `proxy.properties` 文件存在且配置正确
3. **证书文件**：如果使用 HTTPS 代理，确保证书文件路径正确
4. **端口占用**：确保配置的端口未被其他程序占用
5. **日志文件**：定期清理日志文件，避免占用过多磁盘空间
6. **JVM 参数**：启动命令已自动包含 `--add-opens java.base/java.nio=ALL-UNNAMED` 参数，用于支持 netty 直接内存监控

## 性能优化建议

1. **JVM 参数**：可以在 `proxy.service` 或启动脚本中添加 JVM 参数
2. **日志级别**：根据需求调整日志级别
3. **资源限制**：systemd 服务已配置 `LimitNOFILE=100000`

## 卸载服务

```bash
# 停止服务
sudo systemctl stop proxy

# 禁用自启
sudo systemctl disable proxy

# 删除服务文件
sudo rm /etc/systemd/system/proxy.service

# 重新加载 systemd
sudo systemctl daemon-reload

# 删除安装目录（可选）
sudo rm -rf /opt/proxy
```

